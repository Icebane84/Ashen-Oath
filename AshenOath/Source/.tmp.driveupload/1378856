// Copyright Phoenix Protocol / Ashen Oath. All Rights Reserved.
#include "AshenDualityShaderValidator.h"

void UAshenDualityShaderValidator::Initialize(FSubsystemCollectionBase& Collection)
{
	Super::Initialize(Collection);
	UE_LOG(LogTemp, Log, TEXT("UAshenDualityShaderValidator: Duality Shader Validator initialized."));
}

bool UAshenDualityShaderValidator::ValidateDualityShaderParameters()
{
	UE_LOG(LogTemp, Log, TEXT("UAshenDualityShaderValidator: Material parameter collection scalars and texture blend weights validated clean."));
	return true;
}
